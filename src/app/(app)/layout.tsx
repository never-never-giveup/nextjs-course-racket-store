import { Layout } from '@/components/layout'
import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Товары для тенниса',
  description: 'Лучшие товары для тенниса',
}

export default Layout
